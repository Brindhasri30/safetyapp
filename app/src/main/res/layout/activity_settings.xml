<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp"
    android:background="#F6F7FB">

    <!-- Title -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Settings"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="#212121"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="40dp"/>

    <!-- Guardian 1 -->
    <EditText
        android:id="@+id/etGuardian1"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Guardian 1 Phone Number"
        android:inputType="phone"
        android:maxLength="10"
        android:padding="12dp"
        app:layout_constraintTop_toBottomOf="@id/tvTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="32dp"
        android:background="@drawable/rounded_edittext"/>

    <!-- Guardian 2 -->
    <EditText
        android:id="@+id/etGuardian2"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Guardian 2 Phone Number"
        android:inputType="phone"
        android:maxLength="10"
        android:padding="12dp"
        app:layout_constraintTop_toBottomOf="@id/etGuardian1"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"
        android:background="@drawable/rounded_edittext"/>

    <!-- Save Guardians Button -->
    <Button
        android:id="@+id/btnChangeGuardians"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Save Guardians"
        android:textColor="#FFFFFF"
        android:backgroundTint="#455A64"
        android:textAllCaps="false"
        app:layout_constraintTop_toBottomOf="@id/etGuardian2"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp"/>

    <!-- Logout Button -->
    <Button
        android:id="@+id/btnLogout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Logout"
        android:textColor="#FFFFFF"
        android:backgroundTint="#C62828"
        android:textAllCaps="false"
        app:layout_constraintTop_toBottomOf="@id/btnChangeGuardians"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"/>
</androidx.constraintlayout.widget.ConstraintLayout>
